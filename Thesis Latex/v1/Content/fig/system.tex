\begin{tikzpicture}[->,>=stealth',shorten >=1pt,auto,
node distance=3cm,
semithick,scale=0.8, transform shape,scale=0.6, font=\huge]

\tikzstyle{every state}=[rectangle, text width=4cm, minimum height=1.5cm, text centered, 
fill=blue!20,draw=none,text=black, draw,line width=0.3mm]

%start state
\node[state]
(cinput)
{Input}; 

\node[state]
(ccomp) [below of=cinput]
{Computation};

\node[state]
(cprice) [below of=ccomp]
{Emit Outputs}; 

\path[->] (cinput) edge (ccomp);
\path[->] (ccomp) edge (cprice);

\draw[->] (cprice.south)  
	|- ($(cprice.east) + (2cm, -1.5cm)$) 
	-| ($(cinput.east) + (2cm, 1.5cm)$) 
	-| (cinput.north);
	
\end{tikzpicture}
